import { ConvertDegreePipe } from './convert-degree.pipe';

describe('ConvertDegreePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDegreePipe();
    expect(pipe).toBeTruthy();
  });
});

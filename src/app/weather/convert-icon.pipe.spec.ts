import { ConvertIconPipe } from './convert-icon.pipe';

describe('ConvertIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertIconPipe();
    expect(pipe).toBeTruthy();
  });
});
